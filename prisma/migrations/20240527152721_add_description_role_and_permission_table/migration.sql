BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[mnt_permiso] ADD [descripcion] NVARCHAR(1000);

-- AlterTable
ALTER TABLE [dbo].[mnt_rol] ADD [descripcion] NVARCHAR(1000);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
